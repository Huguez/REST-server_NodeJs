const Category = require( "./category" )
const Role = require( "./role" )
const Server = require( "./server" )
const User = require( "./user" )
const Product = require( './producto' )
const { Mensaje, ChatMensajes } = require( "./chatInfo" )

module.exports = {
   Category,
   User,
   Role,
   Server,
   Product,
   Mensaje, 
   ChatMensajes
}